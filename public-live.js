"use strict";(()=>{const CFG=window.WTS_CONFIG,$=s=>document.querySelector(s);async function load(){try{const r=await fetch(`${CFG.supabaseUrl}/rest/v1/rpc/school_registry_public_live_summary`,{method:"POST",headers:{"Content-Type":"application/json",apikey:CFG.publishableKey},body:"{}"});const d=await r.json();if(!r.ok||d?.ok===false)throw new Error(d?.code||"LIVE_SUMMARY_FAILED");if($("#people"))$("#people").textContent=d.people||0;if($("#studentCount"))$("#studentCount").textContent=d.active_students||0;if($("#staffCount"))$("#staffCount").textContent=d.active_staff||0;const dot=$("#dot"),text=$("#connectionText"),notice=document.querySelector('.notice');if(dot)dot.classList.add("on");if(text)text.textContent="Live database";if(notice)notice.innerHTML='<b>Live system:</b> this dashboard is reading the shared WTS registry database. Login is required only for private records and changes.';}catch(e){const text=$("#connectionText");if(text)text.textContent="Live data unavailable";}}load();setInterval(load,60000);})();